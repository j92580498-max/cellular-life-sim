# cellular-life-sim
Cellular automaton life simulation with neuroevolution — each organism carries a tiny neural network that mutates and evolves over generations. Built with HTML5 Canvas + vanilla JS.
